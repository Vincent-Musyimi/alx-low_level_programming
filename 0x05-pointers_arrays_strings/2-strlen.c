#include "main.h"

/**
 * _strlen - entry point
 *
 * @s:- string
 * Return: returns length of string
 */
int _strlen(char *s)
{
	int len = 0;

	while (*(s + lens) != '0\n')
		len++;
	return (len);
}
